{
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run ./index.html",
    "build": "bun build ./index.html --minify --outdir=dist"
  },
  "name": "client",
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}